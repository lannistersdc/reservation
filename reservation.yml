config:
  target: 'http://localhost:3002'
  phases:
    - duration: 60
      arrivalRate: 100
    # - duration: 90
    #   arrivalRate: 5
    #   rampTo: 300
    # - duration: 180
    #   arrivalRate: 300
scenarios:
  - flow:
      - loop:
          - get:
              url: '/api/reservation/id/{{$randomNumber(1,10000000)}}'
